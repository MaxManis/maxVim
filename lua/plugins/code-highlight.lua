return {
	"RRethy/vim-illuminate",
	config = function()
		-- NOTE: Remove any vim-illuminate configuration from your init file,
		-- simply having the plugin installed will load it and start highlighting the buffers using one of LSP, Tree-sitter,
		-- or regex matching (in that order) depending on which is available.

		-- require("illuminate").setup({})
	end,
}
